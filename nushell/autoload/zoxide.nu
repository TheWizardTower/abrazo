# zoxide — smarter cd. Provides `z` and `zi` commands.
# Cached init at ~/.cache/zoxide/init.nu (regenerate with `zoxide init nushell | save -f ~/.cache/zoxide/init.nu`).
source ~/.cache/zoxide/init.nu
